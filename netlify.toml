[build]
  publish = "."
  functions = "netlify/functions"

# The frontend calls /api/chat; Netlify serves functions from /.netlify/functions/.
[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200
  force = true
